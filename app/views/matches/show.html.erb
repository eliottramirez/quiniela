<section class="section">
  <div class="container is-max-desktop">

    <%# notice %>

    <nav class="breadcrumb" aria-label="breadcrumbs">
      <ul>
        <li><%= link_to "Calendario", matches_path %></li>
        <li class="is-active"><a href="#" aria-current="page"><%= "Partido #{@match.id}" %></a></li>
      </ul>
    </nav>

    <div class="columns is-vcentered is-gapless is-mobile">
      <div class="column is-narrow mr-3">
        <h1 class="title"><%= "Partido #{@match.id}" %></h1>
      </div>

      <div class="column is-narrow">
        <%= link_to edit_match_path(@match) , class: "button is-link is-inverted" do %>
          <%= render 'heroicons/pencil_outline' %>
        <% end %>
      </div>

      <div class="column is-narrow">
        <%= button_to @match, method: :delete, class: "button is-inverted is-danger" do %>
          <%= render 'heroicons/trash_outline' %>
        <% end %>
      </div>
    </div>
  </div>

  <div class="container is-max-desktop">
    <div class="box mt-4">
      <%= render @match %>
    </div>
  </div>
</section>
